# SOP Fan-In: MQTT

> **Removed.** The SOP run side this page described (the run engine, its
> event fan-in, run tools, and run state) was removed from ZeroClaw, so the
> rest of this historical page no longer matches the product. SOP definitions
> are still authored and validated here; runs live Tachi-side as ProcedureRuns
> through the procedure_v1 seam. See [Syntax](../syntax.md) for the definition
> format that remains.
