# Wave A Upstream Intake 计划

## 基线（2026-08-04，已验证）

- fork master: `149f682ed`（含 #30/#40/#41）
- upstream master: `274cfd9ec`
- merge-base: `f3023663a`
- upstream 领先 110 commits，fork 领先 74 commits

## 冻结与分支策略（按舰长给的 9 步流程）

```
1. git tag pre-upstream-intake-20260804 origin/master    ← 恢复点
2. git fetch upstream                                      ← 已完成
3. git branch audit/upstream-20260804 upstream/master     ← 只读观察分支
4. git checkout -b sync/wave-a-20260804 origin/master     ← Wave A 工作分支
```

每个 commit 单独 cherry-pick，单独验证，最后一个 PR 合入。不开多 commit 的混合 PR。

## 四个 Wave A commit 的处置（基于子代理 file:line 冲突评估）

### commit 3 先做：`404c3e48d` — Responses usage propagation
- **冲突风险：LOW（最干净）**
- 改 `openai.rs`、`openai_codex.rs`，fork 这两个文件自 merge-base 起零改动
- `git cherry-pick 404c3e48d`
- 验证：`cargo check -p zeroclaw-providers`
- **为什么先做**：commit 2 的 openai.rs diff base 包含 commit 3 的改动，必须先 pick 3 再 pick 2

### commit 2：`4770420ab` — SSE completion/idle-timeout hardening
- **冲突风险：LOW**
- 改 `anthropic.rs`、`compatible.rs`、`openai.rs`、docs。fork 只动过 `compatible.rs` 的 22 行（在 38/546/908/2446/3554 行），上游 commit 改的是 18/1695/3757 行——**hunk 不重叠**
- `git cherry-pick 4770420ab`（在 commit 3 之后）
- 验证：`cargo check -p zeroclaw-providers` + `cargo test -p zeroclaw-providers --lib`
- **fork 不变式**：确认不破坏 429 rotation（这个 commit 不碰 `reliable.rs`）

### commit 1：`1e99541d1` — MCP stdio multiplexing（最大的一个）
- **冲突风险：LOW，但是 merge commit**
- 改 `mcp_client.rs`（+1606）、`mcp_transport.rs`（+1705）、`orchestrator/mod.rs`（test fixture）
- fork 对 `mcp_client.rs` / `mcp_transport.rs` 自 merge-base 起 **零改动**（byte-identical）
- orchestrator/mod.rs 的 17 行 test fixture 改动在 fork 未修改的区域
- **特殊性**：这是 squash-merge 的上游 PR（#9418），可能有多个 parent。如果 `cherry-pick` 报「is a merge」错误，用 `-m 1` 选 first-parent
- 验证：`cargo test -p zeroclaw-tools --lib mcp`（8 不变量测试都在上游的 commit 里）
- **这是 Wave A 价值最高的一个**——它 cover 了 fork PR #13 的全部意图

### commit 4：`53129fc28` — tool-call/result pairs on context retry
- **冲突风险：HIGH —— 不 cherry-pick，语义重做**
- fork 的 reliable.rs 有 339+/37- 的 429 rotation patch（841-1936 区间）
- 这个 commit 重写 reliable.rs 的 1280-1754 区间（~301 行）——**直接撞 fork 的 rotation 代码**
- **处置**：不在 Wave A 做。开一个独立的 residual issue，标注「需要手动合并 tool-pair retry 逻辑 + 429 rotation 到同一个 `impl ModelProvider`」。等 Wave A 的其余三个落定、reliable.rs 稳定后再单独处理

## 执行顺序

```
sync/wave-a-20260804 (基于 origin/master 149f682ed)
  ├─ cherry-pick 404c3e48d   (Responses usage, LOW)
  ├─ cherry-pick 4770420ab   (SSE hardening, LOW, 在 404 之后)
  ├─ cherry-pick 1e99541d1   (MCP multiplexing, LOW, 大但是干净)
  ├─ cargo test 验证全绿
  └─ 开一个 PR：feat(intake): Wave A — MCP transport + provider SSE/Responses reliability

commit 4 不在这个 PR 里——开 residual issue
```

## fork 不变式检查（每个 commit pick 后）

- `always_ask` 仍覆盖 Full autonomy（#37 的逻辑，不在这些 commit 的 diff 里，应不受影响）
- 429 rotation 仍在（commit 2 不碰 reliable.rs，commit 1/3 不碰 providers）
- `allowed_tools` Option 语义不变（这些 commit 不碰 policy.rs）
- MCP `__` auto-admit 仍 opt-in（commit 1 是上游的干净实现，fork 的 hardening 在别的文件）

## 验证命令

```bash
CARGO_TARGET_DIR=<task-scoped> cargo check --workspace
CARGO_TARGET_DIR=<task-scoped> cargo test -p zeroclaw-tools --lib mcp
CARGO_TARGET_DIR=<task-scoped> cargo test -p zeroclaw-providers --lib
CARGO_TARGET_DIR=<task-scoped> cargo clippy --all-targets --no-deps -- -D warnings
cargo fmt --all -- --check
```

## STOP 条件

- cherry-pick 出现评估之外的冲突（非预期的 hunk 重叠）→ 停，报告，不强推
- pick 后 cargo test 出现非测试自身的新 failure → 停，回退
- reliable.rs 被意外波及 → 停（这是 commit 4 的领地，不该在 Wave A 触碰）

## 不做的事

- 不整树合并上游 110 commits
- 不在这个 PR 里做 commit 4（HIGH 风险，单独 residual）
- 不做 Wave B/C/D（这是 Wave A 专用 PR）
- 不改 CI（#28 仍未解，CI 会红，但本地验证全绿即可作为 evidence）